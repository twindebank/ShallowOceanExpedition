pytest==3.6.2
pytest-cov==2.5.1
pytest-pycodestyle==1.2.2
# flake8 doesn't currently work with 2.4.0
# if that changes in future then this version can be changed.
pycodestyle<2.4.0
flake8==3.5.0
coveralls==1.3.0